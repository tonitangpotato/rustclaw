# 2026-04-04

## Sub-agent Cancel Bug
- potato 指出：spawn 的 sub-agent session key 不在主 agent 的 cancellation_tokens 里，/stop 无法 kill sub-agent
- 三层问题：sub-agent 没注册 CancellationToken、loop 里不检查 cancellation、/stop 不知道子 session key
- 修复方案：注册 token + loop 检查 + parent→children 映射级联 cancel
- 交给 OpenClaw 修

## Requirements 文档拆分原则（通用）
- 单个 requirements 文档 ≤15 GOALs，超过必须拆成 feature-level 文档
- Master doc: overview + feature index + GUARDs only
- Feature docs: 10-15 GOALs each
- 这是 review 质量的根本保障，不需要两层 review agent 架构
- 已更新 `skills/draft-requirements/SKILL.md` 和 `skills/review-requirements/SKILL.md`
