
## ISS-002 Fix: Autopilot Spin-Loop (RustClaw)
- **Problem**: 60-turn spin loop on write-heavy tasks, burning 150-200万 tokens with zero output
- **Fix**: Max 3 smart attempts per task (was 60 blind "Continue" messages)
  - Attempt 1: full prompt, let tool loop complete
  - Attempt 2: contextual "previous attempt didn't complete" 
  - Attempt 3: final attempt with strong language
- **Commit**: c7d6a04 (rustclaw)
- Tests: 5 pass

## ISS-008 Feature: Knowledge Promotion (engram)
- **New module**: `src/promotion.rs` — detect high-activation memory clusters for promotion
- **Algorithm**: Core-layer memories → Hebbian adjacency → BFS components → filter by size/span/importance → suggest target doc
- **Storage**: `promotion_candidates` table + CRUD methods
- **API**: `Memory::detect_promotion_candidates()`, `pending_promotions()`, `resolve_promotion()`
- **Config**: `PromotionConfig` (opt-in, default disabled)
- **Commit**: cde00f1 (engram)
- Tests: 8 new, 800 total

## Issue Status Updates (engram)
- ✅ ISS-009 → closed (entity_recall implemented + ISS-016 triple extraction)
- ✅ ISS-015 → closed (Infomap already in synthesis/cluster.rs + infomap-rs crate)
- ✅ ISS-016 → closed (committed previously)
- ✅ ISS-008 → closed (just implemented)
- Remaining open: ISS-004 (P2), ISS-013 (P3), ISS-014 (P2)
